# LOLI取り込み記録（各国インベントリ）

外部データベース LOLI から、各国のインベントリ（既存化学物質名簿）を取り込んだ記録。
**何をどこから取り、番号がどう入っているか**を残すためのもの。

| | |
| --- | --- |
| 取り込み日 | 2026-08-26 |
| 元データ | `LOLI4_Datafeed_2026Q3`（社内SQL Server `192.168.1.153`） |
| 名簿 | 12件（SDS 第15項で在否を書く国） |
| 行数 | **983,214行** |

取り出しは `scripts/sql/loli-inventories.sql`、取り込みは `scripts/seed-inventories.ts`。

---

## 1. 規制区分としては持たない

**インベントリは `regulation_categories` に入れていない。**

判定は登録されている区分をすべて見に行くので、インベントリを区分として入れると
**どの製品もすべてのインベントリに「該当」する**。判定表が使い物にならなくなる。

インベントリは「載っているか」と「その名簿での番号」を持つだけのもので、
含有率と閾値で当たりを決める規制とは性質が違う。
`inventories` と `inventory_rows` という別の表に持つ。

## 2. LOLI の `Data` をそのまま持つ

番号の取り出しは、名簿ごとの**正規表現**（`inventories.match_pattern`）を当てて行う。
取り込みでは加工しない。

こうしてある理由は、**本番から LOLI に届かない**ため。
取り出しかたを直したくなったとき、加工済みの値しか持っていないと
手元で取り込み直すまで直らない。元の文字列があれば、設定を直せばその場で変わる。

---

## 3. 取り込んだ名簿と、番号の入りかた

| 名簿 | 国 | LOLI | 行数 | `Data` の形 | 取り出し |
| --- | --- | ---: | ---: | --- | --- |
| ENCS（化審法） | 日本 | 622 | 90,075 | `(5)-3714` | `\((\d+)\)-(\d+)` |
| ISHL（安衛法） | 日本 | 3830 | 103,797 | `Present (3)-1011 (ENCS inventory number...)` | 同上 |
| EINECS | EU | 115 | 127,636 | `202-906-3` | `(\d{3}-\d{3}-\d)` |
| TSCA | 米国 | 100 | 111,394 | `Present (ACTIVE)` | `\((ACTIVE\|INACTIVE)\)` |
| DSL | カナダ | 101 | 55,148 | `Present` | なし（「該当」） |
| NDSL | カナダ | 102 | 62,200 | `Present` | なし（「該当」） |
| IECSC | 中国 | 740 | 71,456 | `Present [28742]` | `\[(\d+)\]` |
| KECI | 韓国 | 633 | 65,746 | `Present [KE-05780]` | `(KE-\d+)` |
| TCSI | 台湾 | 6575 | 112,656 | `Present` | なし（「該当」） |
| AIIC | 豪州 | 620 | 71,577 | `Present` | なし（「該当」） |
| PICCS | フィリピン | 621 | 51,393 | `Present` | なし（「該当」） |
| NZIoC | NZ | 3005 | 60,136 | `HSNO Approval: HSR003056 (...)` | `(HSR\d+)` |

国が無かった EU・台湾・フィリピンは、このときに足した。

---

## 4. 番号の扱いで気をつけたこと

### 1つのCASに複数の番号が付く

EINECS と KECI で起きる。同じ物質が別名で2回登録されている。

```
EINECS  100-78-7  "215-637-1" As 1,2,3-Propanetriol, acetate [1335-58-6];
                  "247-704-6" As Glyceryl monoacetate [26446-35-5]
KECI    100-78-7  "Present [KE-29298]" As ...;  "Present [KE-29309]" As ...
```

**正規表現を全件一致で当てて、取れた数だけ出す。**どちらか一方は選べない。

（`XML` カラムには `<row>` ごとに `<inv_num>` が入っており、そちらでも分けられる。
ただし管理者が LOLI 側で目にするのは `Data` の文字列なので、そちらに当てている）

### TSCA は「該当」と出してはいけない

`value` はすべて `Present` だが、状態が別に入っている。

```
ACTIVE     78,043件
INACTIVE   39,507件
```

**INACTIVE は「名簿には載っているが、いま米国で商業流通していない」**という意味。
一律「該当」と出すと、そのまま輸出できるように読まれる。
`ACTIVE` / `INACTIVE` をそのまま出している。

### NZIoC も「該当」と出してはいけない

6割が「個別の承認は無い」と書かれている。

```
個別の承認は無いが、グループ基準の下で使用可能   39,686件
個別の承認は無いが、〜の成分としてなら使用可能   22,840件
承認あり（条件付き）                              866件
非有害                                            223件
```

**承認番号（`HSR…`）を持つ行だけを出す。**実データでは11%しか一致しない。
残りは「載っているが個別承認は無い」ので、番号としては出さないのが正しい。

### 番号を持たない行がある

ENCS で 3,808行、ISHL で 2,294行の番号が `-`。
「既存化学物質等として扱う」「日本薬局方に収載」などの理由だけが書かれている。
正規表現に一致しないので、そのまま出ない（一致率は ENCS 95%・ISHL 96%）。

---

## 5. 次に取り込むときに見ること

- **一致率**。設定画面の「試す」で実データに当てて確かめる。
  0%になっていれば LOLI 側の書き方が変わった合図
- TSCA の `INACTIVE` の割合。増えていれば、輸出先での扱いに影響する
- 1つのCASに複数番号が付く行の数。増えていれば名寄せの見直しが要る
- **`scripts/seed-substances-from-links.ts` は流さないこと。**
  インベントリは物質マスタとは別の表に持っており、CASを物質マスタへ足す対象ではない
