# dependencies
node_modules/

# next.js
.next/
/out/
apps/*/out/
next-env.d.ts

# typescript
*.tsbuildinfo

# build
dist/
coverage/

# env（機密・接続情報はコミットしない: CLAUDE.md §8）
.env
.env.local
.env.*.local
.env.prod

# misc
*.log
.DS_Store

# LOLI（社内SQL Server）への接続。手元だけのもの
.env.loli

# 照合に使う法令XMLの控え（scripts/verify-jp-laws.ts が作る）
.cache/

# 目録の取り出し結果は大きすぎる（60MB超）。取り出し直せるので追わない
scripts/data/loli-inventories.tsv

# LOLI の ListData.Data の取り出し（60MB 超。scripts/loli-dump-data.sh で作り直せる）
scripts/data/loli-data-*.tsv
