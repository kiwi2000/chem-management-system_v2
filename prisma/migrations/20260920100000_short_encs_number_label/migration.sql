-- インベントリの呼び名を短くする（2026-09-20 指示）。
--
-- 「化審法番号（官報公示整理番号）」は列の見出しとしては長すぎ、
-- 2行に折り返して表を押し広げていた。「化審法番号」で通じる。
-- 呼び名は画面（インベントリ）から変えられるが、**どの環境でも同じ見出しにしたい**ので
-- 移行で揃える。すでに手で短くしてあれば何も起きない
UPDATE "inventories"
SET "number_label" = '化審法番号'
WHERE "number_label" = '化審法番号（官報公示整理番号）';
