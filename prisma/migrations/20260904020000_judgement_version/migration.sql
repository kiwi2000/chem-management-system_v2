-- 判定に「どの法規制バージョンで出したか」を持たせる。
--
-- 法規制のデータ（CASリンク・閾値・バージョン）を変えても判定は自動でやり直されない。
-- いつ・どの前提で出した判定かを画面に示し、前提が変わっていれば注意を出すために要る。
-- 以前の判定は分からないので NULL のまま（次に判定し直したときに入る）。
ALTER TABLE "product_judgements" ADD COLUMN "version_id" TEXT;
