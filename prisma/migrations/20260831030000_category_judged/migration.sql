-- 区分を判定に使うかどうかの印。
--
-- **候補の一覧を「持つだけ」にできるようにする。**
-- 中環審答申の有害大気汚染物質のように、含有率で該非が決まるのではなく
-- 「該当する可能性がある物質」を並べたものがある。判定に出すと
-- ほとんどの製品で該当が並び、本当に効く規制が埋もれる。
--
-- **いまある区分はすべて判定に使う。**既定を true にして、動きを変えない
ALTER TABLE "regulation_categories"
  ADD COLUMN "judged" BOOLEAN NOT NULL DEFAULT true;
