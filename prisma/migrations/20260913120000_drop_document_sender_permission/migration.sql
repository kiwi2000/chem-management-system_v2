-- 「ドキュメントの差出人を選べる」権限は要らなくなった（2026-09-13 指示）。
-- 差込項目を「所属する会社名／所属部署」（いつも作った人のもの）と
-- 「任意の会社名／任意の部署」（作るときに誰でも選べる）に分けたため。
-- enum の値そのものは Postgres では簡単に消せないので残し、付与されている行だけ消す。
-- アプリ側は知らない値として読み飛ばす
DELETE FROM "user_permissions" WHERE "permission" = 'DOCUMENT_SENDER';
