-- 規制区分に「適用開始日」「適用終了日」を足す。
--
-- 規制が変わったら区分を丸ごと新しく作り、古い区分も残す決まり（2026-09-08）のため、
-- 「この区分はいつからいつまで効くか」を区分そのものに持たせる。
-- 判定対象日を指定した判定では、その日に効いている区分と法文物質名だけを使う。
-- 空なら常に効く。保存してある判定（今日のもの）は、これまでどおり日付で絞らない。
ALTER TABLE "regulation_categories"
  ADD COLUMN "effective_from" DATE,
  ADD COLUMN "effective_to" DATE;
