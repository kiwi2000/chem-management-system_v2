/**
 * 1件ものの画面（製品・物質・利用者の詳細と新規登録）の外枠。
 *
 * **本文の枠いっぱいに広げる。**中身は表が主で、列が多い。
 * 幅を絞ると表だけが横スクロールし、画面の両脇は空いたまま、という形になる。
 *
 * 余白は本文の枠との間に取る（画面の端に文字が貼り付かないように）。
 *
 * **同じ形の画面はすべてこれを使うこと。**画面ごとに書くと、
 * 直したときに一部だけ取り残されて、画面を行き来したときに幅が跳ねる。
 */
export const PAGE_SHELL = "w-full p-4 lg:p-6";

/** 上のものに、節どうしの間隔を足したもの。ほとんどの画面はこちら */
export const PAGE_SHELL_STACKED = `${PAGE_SHELL} space-y-4`;
