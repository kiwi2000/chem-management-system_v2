import { List, Note, P, PageHead, Section, SpecTable, StateChip, Sub, T } from "../_parts";
import { SpecPager } from "../spec-pager";

export default function SpecListsPage() {
  return (
    <>
      <PageHead
        title="一覧とフィルター"
        lead="物質や製品の一覧は、どの画面でも同じ操作で扱えるようにしてあります。並べ替え・絞り込み・1ページの件数は、次に開いたときも覚えています。"
      />

      <Section title="一覧が上下2つに分かれている理由">
        <P>
          物質と製品の一覧は、上下2つの表に分かれています。
          <T>上が、みんなが使える公開済のもの</T>。<T>下が、まだ仕上がっていないもの</T>です。
        </P>
        <SpecTable
          head={["", "中身", "見出し", "見える人"]}
          rows={[
            [
              "上",
              <StateChip key="p" tone="published">
                公開済
              </StateChip>,
              "なし（画面の見出しがそのまま）",
              "見る権限があれば誰でも",
            ],
            [
              "下",
              <span key="w" className="flex flex-wrap gap-1">
                <StateChip tone="draft">作成中</StateChip>
                <StateChip tone="pending">承認待</StateChip>
                <StateChip tone="rejected">却下</StateChip>
              </span>,
              "作業中",
              "未公開のものを見られる人、編集できる人、承認できる人",
            ],
          ]}
        />
        <List
          items={[
            "上の表には「状態」の列を出しません。全部同じ値になるためです。",
            "下の表が出ない人には、これまでどおり普通の一覧が1つ見えます。",
            "状態を変えると下から上へ移るので、両方を読み直します。",
            "下の表では、行を選んでまとめて申請・発行ができます。却下されたものは赤い太字で目立たせます。",
          ]}
        />
        <Note>
          上の表に見出しを付けないのは、公開済が既定の一覧だからです。
          そこに名前を付けると、かえって「別のもの」に見えてしまいます。
        </Note>
      </Section>

      <Section title="フィルター">
        <P>
          「フィルター」を開くと、条件を入れる欄が出ます。項目ごとに、
          「を含む」「と一致する」といった条件の種類を選べます。
        </P>
        <Sub title="チェックボックスの条件">
          <P>
            「有効 / 無効」「原材料 / 原材料以外」は、2つのチェックボックスで指定します。
            <T>どちらもオンが初期状態</T>で、そのときは全部が対象です。
            片方だけオンにすると、そちらだけに絞られます。
          </P>
        </Sub>
        <Sub title="組成の条件（CAS番号）">
          <P>
            製品の一覧では、中に入っている物質のCAS番号で絞り込めます。
            複数行の入力欄にCAS番号を並べて書きます。数字とハイフン以外は区切りとみなすので、
            改行・カンマ・空白のどれで区切ってもかまいません。
          </P>
          <P>
            並べたCAS番号について、<T>いずれかを含む</T>か<T>すべてを含む</T>かを選べます。
            番号は完全一致で見ます（部分一致はしません）。
          </P>
        </Sub>
        <Sub title="法規情報の条件">
          <P>該当法規で絞り込む欄を置く予定ですが、いまは保留です。</P>
        </Sub>
      </Section>

      <Section title="フィルターの保存と読込">
        <P>
          よく使う条件は名前を付けて保存でき、「読込」から呼び出せます。
          一覧のある画面すべてで同じように使えます。
        </P>
        <List
          items={[
            "同じ画面で同じ名前を付けると、上書きになります。",
            "「全員が使えるようにする」を選ぶと、他の人からも読み込めます。共有したものには作成者の名前が付きます。",
            "削除できるのは、作った本人とシステム管理者です。",
          ]}
        />
      </Section>

      <Section title="表の扱い">
        <List
          items={[
            "列の見出しを押すと並べ替えます。",
            "行をダブルクリックすると詳細を開きます。",
            "1ページの件数を選べます。",
            "並べ替え・絞り込み・件数は画面ごとに覚えていて、次に開いたときも同じ状態から始まります。",
            "1つの画面に表が2つある場合（公開済と作業中）は、それぞれ別に覚えます。",
          ]}
        />
      </Section>

      <SpecPager current="/spec/lists" />
    </>
  );
}
