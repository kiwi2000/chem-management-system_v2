import { Note, P, PageHead, Section, SpecTable, Sub, T } from "../_parts";
import { SpecPager } from "../spec-pager";

/**
 * 画像ライブラリ。
 *
 * 読み手はテンプレートを整える人。ロゴ・印影・写真を入れて、テンプレートの「画像」ブロックで使う。
 */
export default function ManualImagesPage() {
  return (
    <>
      <PageHead
        title="画像ライブラリ"
        lead="帳票に載せるロゴ・印影・署名・写真を置いておく場所です。1つの画像を複数のテンプレートで使えます。"
      />

      <Section title="画像を入れる">
        <P>
          左のメニューの<T>「ドキュメント」→「画像ライブラリ」</T>を開き、
          <T>「アップロード」</T>を押してファイルを選びます。<T>まとめて何枚でも</T>選べます。
        </P>
        <SpecTable
          head={["受け付ける形式", "1枚の上限"]}
          rows={[["PNG・JPEG・GIF・WebP・BMP・TIFF", "20 MB"]]}
        />
        <P>
          入れるときに、<T>システム設定の「画像」で決めた決まり</T>で自動で整えます
          （大きすぎる画像は縮め、形式を PNG か JPEG にそろえます）。
          元のファイルはそのままにはならず、 整えたものだけが残ります。
        </P>
        <Note title="どの形式で入れればよいか">
          ロゴ・印影・署名のように<T>線と面でできたものは PNG</T>
          （にじまず、背景を透明にできます）、
          <T>写真は JPEG</T>が向いています。印刷で 300dpi を保つなら、幅 50mm のロゴで約 600px、 A4
          いっぱいの幅で約 2,100px あれば足ります。それより大きくしてもきれいにはなりません。
        </Note>
      </Section>

      <Section title="一覧でできること">
        <SpecTable
          head={["したいこと", "操作"]}
          rows={[
            ["名前や備考を直す", "行の右端の鉛筆を押します。Enter で保存、Esc でやめます"],
            ["消す", "行の左のチェックを付けて、表の左上のごみ箱を押します"],
            [
              "どこで使っているかを知る",
              "「使用テンプレート」の欄に、その画像を置いているテンプレートの数が出ます",
            ],
          ]}
        />
        <Note title="使っている画像は消せません">
          テンプレートの「画像」ブロックで使っている画像は、消そうとすると断られます
          （紙面に穴が空くため）。先にテンプレートのほうで外してください。
        </Note>
      </Section>

      <Section title="テンプレートに置く">
        <P>
          テンプレート編集で<T>「＋ 画像」</T>を押すとブロックができます。
          <T>「ライブラリから選ぶ」</T>で一覧（小さな絵）から選ぶか、その場でアップロードします。
        </P>
        <SpecTable
          head={["欄", "意味"]}
          rows={[
            [
              "幅（mm）・高さ（mm）",
              "片方だけ入れると、もう片方は縦横の比で決まります。両方空なら元の大きさ（紙幅を超えれば縮みます）",
            ],
            ["寄せ", "ブロックの中で左・中央・右のどこに置くか"],
            [
              "横幅",
              "ほかのブロックと同じです。50% にして隣に文章を置く、といった使い方ができます",
            ],
          ]}
        />
        <Sub title="項目ブロックのラベル">
          <P>
            同じ日に、「項目」ブロック（ラベルと値の並び）に<T>「ラベルの位置」（左／右）</T>と
            <T>「ラベルの幅（%）」</T>が増えました。幅はブロックの横幅に対する割合で、
            空ならラベルの長さぶんだけ取ります。値を右に、ラベルをその右に、といった並びも作れます。
          </P>
        </Sub>
      </Section>

      <SpecPager current="/spec/images" />
    </>
  );
}
