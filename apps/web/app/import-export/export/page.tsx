import { UnderConstruction } from "@/components/under-construction";

/** まだ作っていない画面。メニューから押せるので、作っていないことだけを伝える */
export default function Page() {
  return <UnderConstruction titleKey="dataExport" />;
}
